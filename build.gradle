allprojects {
    apply plugin: 'java'

    repositories {
        jcenter()
    }
}

task run {
    println "test"
}

subprojects {
    ext {
        log4jVersion = '2.11.1'
    }

    dependencies {
        implementation "org.apache.logging.log4j:log4j-core:${log4jVersion}"
    }
}